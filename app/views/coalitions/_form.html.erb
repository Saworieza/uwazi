<%= form_for @coalition, html: { multipart: true } do |f| %>
  <% if @coalition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@coalition.errors.count, "error") %> prohibited this coalition from being saved:</h2>

      <ul>
      <% @coalition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :abbrev %><br>
    <%= f.text_field :abbrev %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
